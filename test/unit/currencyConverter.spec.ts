describe('Currency converter', () => {
  it('Should split left hand-side in chunks of three or less digits');
  it('Should round right-hand side of number to two decimals');
  it('Should format integers and decimals as a money string');
});
